repos:
  - repo: https://github.com/oxc-project/mirrors-oxlint
    rev: v1.46.0
    hooks:
      - id: oxlint
  - repo: local
    hooks:
      - id: oxfmt
        name: oxfmt (md/mdx)
        entry: pnpm exec oxfmt --write
        language: system
        files: \.(md|mdx)$
