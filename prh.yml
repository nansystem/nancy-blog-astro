version: 1
rules:
  # 英語的な直訳表現
  - expected: 担う
    patterns:
      - 面倒を見る
  - expected: 処理する
    patterns:
      - ハンドルする
  - expected: 対応する
    patterns:
      - カバーする
  - expected: 構成する
    patterns:
      - セットアップする
  - expected: 実行する
    patterns:
      - ランする
      - 走らせる

  # 固有名詞・技術用語の表記統一
  - expected: PicoClaw
    patterns:
      - picoClaw
      - Picoclaw
      - picoclaw
  - expected: オーケストレーター型
    patterns:
      - Orchestrator型
      - Orchestrator 型
      - orchestrator型
      - オーケストレーション型
  - expected: LLM
    patterns:
      - llm
